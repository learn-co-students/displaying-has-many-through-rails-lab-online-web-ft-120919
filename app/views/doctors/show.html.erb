<h1><%= @doctor.name %></h1>
<h3><%= @doctor.department %></h3>

<h4>Appointments:</h4>

  <ul>
  <% @doctor.appointments.each do |appt| %>
    <li><%= link_to appt.patient.name, patient_path(appt.patient) %> <%= appt.readable_appt_time %></li>
  <% end %>
  </ul>
