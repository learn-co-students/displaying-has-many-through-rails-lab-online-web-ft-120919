<h1><%=@doctor.name%></h1>
<h3><%=@doctor.department%></h3>
<h3>Appointments:</h3>
<% @doctor.appointments.each do |a|%>
  <%= a.appointment_datetime.strftime("%B %d, %Y at %H:%M")%> <%= link_to a.patient.name, patient_path(a.patient) %>
<%end%>
