<h1><%= @patient.name %></h1>

<h4>Appointments:</h4>

  <ul>
  <% @patient.appointments.each do |appt| %>
    <li><%= link_to appt.doctor.name, doctor_path(appt.doctor) %> <%= appt.readable_appt_time %></li>
  <% end %>
  </ul>