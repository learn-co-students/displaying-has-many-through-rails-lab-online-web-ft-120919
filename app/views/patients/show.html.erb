<h1><%= @patient.name %>  </h1>
<h2><%= @patient.age %> yearls old</h2>

<% @patient.appointments.each do |p| %>
    <p>Time: <%= p.appointment_datetime.strftime("%B %d, %Y at %H:%M")%> </p>
    Doctor: <%=link_to p.doctor.name, doctor_path(p.doctor) %> 
    <% end %>