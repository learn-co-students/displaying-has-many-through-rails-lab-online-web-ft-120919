<h1>Doctor: <%= link_to @appointment.doctor.name, doctor_path(@appointment.doctor) %></h1>
<h1>Patient: <%= link_to @appointment.patient.name, patient_path(@appointment.patient) %></h1>
<p>Appointment time:<%= @appointment.appointment_datetime.strftime("%B %d, %Y") %> at <%= @appointment.appointment_datetime.strftime("%k:%M") %></p>